type CartItem {
  id: ID!
  product: Product!
  quantity: Int!
}

type Cart {
  id: ID!
  user: User!
  items: [CartItems!]!
  totalAmount: Float!
}

input AddCartItemInput {
  userId: ID!
  productId: ID!
  quantity: Int!
}

input deleteCartItemInput {
  cartItemId: ID!
  userId: ID!
}
